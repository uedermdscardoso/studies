//Trabalhando com Gulp - nodejs
//Automação de tarefas

//Instalar gulp
//npm install gulp -g    (podemos acessar por meio da nossa linha de comando)
//npm install gulp gulp-uglify  (para trabalhar localmente -- sem a flag 'g') --> gulp-uglify é para minificar arquivos

var abc = 5, 
	cde = 8; 

function teste(){
	return abc+cde;
}